# MongoDB Atlas connection settings (template — copy to .env and fill in real values).
# The real .env file is gitignored; this .example file is safe to commit.
MONGODB_URI="mongodb+srv://<username>:<password>@<cluster>/?appName=<app>"
MONGODB_DB="cd_automation"
MONGODB_COLLECTION="samples"
